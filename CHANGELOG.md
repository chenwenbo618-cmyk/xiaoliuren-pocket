# CHANGELOG

## v1.0

第一版正式可用版本。

### 已完成

- H5 手机端可用，PC 端居中展示。
- 支持北京时间起卦，`datetime-local` 选择的时间一律按北京时间处理。
- 支持公历转农历，显示农历月、日、时辰和闰月信息。
- 支持小六壬月日时起课法，计算月宫、日宫、时宫和最终结果。
- 支持六宫罗盘流转动画，动画结束位置与实际计算结果一致。
- 支持展示传统口诀、传统占语、白话解释和此卦处理方式。
- 支持复制完整起卦结果。
- 支持第一次打开说明弹窗，并通过 localStorage 记录已读状态。
- 支持 AI 详细解读预留按钮，第一版不接入接口。
- 支持构建为纯前端静态项目，`dist/` 可部署到 Cloudflare Pages 或 Vercel。

### 部署说明

- 本地开发使用 `npm run dev`，仅用于调试。
- 正式上线使用 `npm run build` 生成 `dist/`。
- `dist/` 是纯静态前端产物，不依赖本地电脑、不依赖局域网、不需要后台服务器。
- 部署到 Cloudflare Pages 或 Vercel 后，手机访问公网链接不需要和电脑处于同一 Wi-Fi。

### 后续方向

- 历史记录。
- AI 详细解读。
- 生成结果图。
- 微信小程序版。
